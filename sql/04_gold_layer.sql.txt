USE DATABASE ECOM_DB;
USE SCHEMA GOLD;

-- Create Gold View
CREATE OR REPLACE VIEW SALES_GOLD AS
SELECT
    PRODUCT_ID,
    SUM(QUANTITY) AS TOTAL_QUANTITY,
    SUM(QUANTITY * PRICE) AS TOTAL_REVENUE
FROM ECOM_DB.SILVER.SALES_CLEAN
GROUP BY PRODUCT_ID;
