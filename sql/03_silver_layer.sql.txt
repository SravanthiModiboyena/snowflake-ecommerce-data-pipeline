USE DATABASE ECOM_DB;
USE SCHEMA SILVER;

-- Create Clean Table
CREATE OR REPLACE TABLE SALES_CLEAN AS
SELECT
    ORDER_ID,
    PRODUCT_ID,
    QUANTITY,
    PRICE,
    ORDER_DATE
FROM ECOM_DB.RAW.SALES_RAW
WHERE QUANTITY IS NOT NULL
AND PRICE IS NOT NULL;
